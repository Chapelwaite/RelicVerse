{
  "name": "relicverse",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "RelicVerse — ქართული dark fantasy ონლაინ-მაღაზია (frontend-only, Vercel-ready)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "art": "node scripts/generate-art.mjs && node scripts/generate-universe-art.mjs",
    "reset:data": "node scripts/build-seed.mjs && node scripts/generate-art.mjs && node scripts/generate-universe-art.mjs"
  },
  "dependencies": {
    "framer-motion": "^11.15.0",
    "gsap": "^3.15.0",
    "lucide-react": "^0.469.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.28.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7"
  },
  "engines": {
    "node": ">=18"
  }
}
